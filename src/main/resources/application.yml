spring:
  h2:
    console:
      enabled: true
      path: /h2  datasource:
    url: jdbc:h2:file:~/test;INIT=runscript from 'src/main/resources/create.sql';    username: sa
    password: 
    driver-class-name: org.h2.Driver  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect    hibernate:      ddl-auto: create
      